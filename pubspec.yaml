name: myapp
description: A new Flutter project for animal data management.

publish_to: 'none' # Remove this line if you're planning to publish to the Flutter package repository

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  logger: ^1.0.0  # Add the logger dependency here

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

